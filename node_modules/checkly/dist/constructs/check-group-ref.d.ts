import { CheckConfigDefaults } from '../services/checkly-config-loader';
import { Construct } from './construct';
/**
 * Creates a reference to an existing Check Group (both v1 and v2).
 *
 * References link existing resources to a project without managing them.
 */
export declare class CheckGroupRef extends Construct {
    constructor(logicalId: string, physicalId: string | number);
    describe(): string;
    getCheckDefaults(): CheckConfigDefaults;
    getBrowserCheckDefaults(): CheckConfigDefaults;
    getMultiStepCheckDefaults(): CheckConfigDefaults;
    synthesize(): null;
}
