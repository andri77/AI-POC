import { BaseCommand } from './baseCommand';
export default class Rules extends BaseCommand {
    static hidden: boolean;
    static description: string;
    run(): Promise<void>;
    private readBaseRulesFile;
    private confirmOverwrite;
}
